import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    flex: 1,
    margin: 5,
    padding: 3,
    borderRadius: 10,
    alignItems: "center",
    justifyContent: "space-around",
    minWidth: 175,
    minHeight: 100,
  },
  cardText: {
    textAlign: "center",
  },
});
